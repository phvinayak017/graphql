import React,{Component} from 'react'

export const Contact = (props) =>{
    console.log(props.history)
    return(
        <div>
            <h2>
                Contact
            </h2>
        </div>
    )
}